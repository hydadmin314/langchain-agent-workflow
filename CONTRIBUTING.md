# 项目开发规范 · LANGCHAIN-AGENT-WORKFLOW

本项目为企业级 LangChain Agent 工作流脚手架，所有开发人员必须遵守以下规范，保证架构统一、代码可维护、后续可扩展。

---

## 一、目录结构说明（禁止擅自修改）

langchain-agent-workflow/├── agent/ # Agent 核心实现（禁止乱改）├── config/ # 统一配置（环境变量、模型参数）├── langgraph/ # LangGraph 状态、节点、路由逻辑 ├── memory/ # 对话记忆模块 ├── prompts/ # 所有业务提示词 ├── tools/ # 所有业务工具（按模块扩展）├── utils/ # 通用工具、日志 ├── .env # 本地配置（不上传 Git）├── .gitignore├── main.py # 项目入口 ├── README.md└── requirements.txt

---

## 二、开发协作规范

1.  **分支管理规则**

    - `main`：保护主分支，仅可通过 PR 合并，禁止直接提交。
    - `dev`：日常开发集成分支，所有功能分支基于`dev`创建。
    - 个人功能分支命名规则：`feat/业务名-功能点`
      例：`feat/sales-tool` / `feat/hr-prompt`

2.  **提交规范**
    提交信息格式：`类型: 描述`

    - `feat:` 新增功能
    - `fix:` 修复 bug
    - `docs:` 文档更新
    - `refactor:` 重构代码
      例：`feat: 新增销售跟进话术生成工具`

3.  **PR 合入要求**
    - 必须关联对应需求/问题
    - 必须通过本地测试
    - 必须经过管理者审核后方可合并

---

## 三、代码与架构规范

1.  **架构基线不可动**

    - 禁止不经协商修改`agent/`、`config/`、`langgraph/`下的核心框架代码
    - 所有业务扩展必须在`tools/`、`prompts/`、`memory/`中进行
    - 禁止不经协商擅自引入新的 Agent 架构（如自行新增多智能体）

2.  **工具开发规范**

    - 所有业务工具统一放在`tools/`目录
    - 单个工具一个文件，命名：`业务名_工具名.py`
    - 新增工具必须在`tools/tool_list.py`中注册，否则无法被 Agent 调用
    - 工具必须使用`@tool`装饰器，并补充清晰的文档字符串（docstring）

3.  **提示词开发规范**

    - 所有业务提示词放在`prompts/`目录，按业务命名
    - 必须通过统一方法导出（如`get_xxx_prompt()`），禁止在业务代码中硬编码
    - 提示词需明确角色、能力边界、回答格式，避免 Agent 幻觉

4.  **环境与安全规范**

    - API Key、密钥等敏感信息**禁止硬编码在代码中**，必须从`.env`读取
    - `.env`文件不上传 Git，开发人员本地自行配置
    - 禁止在日志中打印用户敏感信息（如密码、手机号、身份证号）

5.  **依赖管理规范**
    - 新增依赖必须通过`uv pip install xxx`安装，并更新`requirements.txt`
    - 禁止使用未在`requirements.txt`中声明的第三方库
    - Python 版本统一为 3.10+

---

## 四、业务扩展流程（标准步骤）

1.  新建功能分支：`git checkout -b feat/xxx dev`
2.  开发业务工具：在`tools/`新建文件，编写并注册
3.  开发业务提示词：在`prompts/`新建文件，编写角色 Prompt
4.  测试本地运行：修改`main.py`临时切换 Agent 模式，本地验证功能
5.  提交 PR 到`dev`分支，等待审核合入
6.  稳定后由项目经理合并`dev`到`main`主干

---

## 五、问题反馈

如遇到架构问题、规范疑问或扩展限制，请直接联系项目经理沟通，禁止擅自修改框架结构。
